<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Your Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  
  <main class="min-h-screen w-full bg-zinc-900 text-white p-10">
    <h3 class="text-3xl font-medium tracking-tight text-teal-500 mb-5">Edit Post</h3>
    <form action="/update/<%= post._id %>" method="post">
      <input class="block mb-3 px-5 py-2 rounded-md bg-transparent border-2 border-zinc-700 outline-none text-zinc-500" type="email" value="<%= post.content %>" readonly name="prevContent">
      <textarea name="content" placeholder="Edit Your Post" class="px-5 py-2 outline-none bg-transparent rounded-md border-2 border-zinc-700 resize-none w-1/3"></textarea>
      <input class="cursor-pointer px-5 py-2 rounded-md bg-yellow-500 block mt-2 text-sm" type="submit" value="Update Post">
    </form>
  </main>
  
  
</body>
</html>